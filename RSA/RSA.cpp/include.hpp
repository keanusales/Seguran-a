#ifndef include_hpp
#define include_hpp

#include <cmath>
using std::floor;
using std::sqrt;

#include <cstdlib>
using std::srand;
using std::rand;

#include <chrono>
using std::chrono::system_clock;

#include <deque>
using std::deque;

#include <iostream>
using std::ostream;
using std::cout;

typedef long long int64;

typedef unsigned uint;
typedef const uint cuint;
typedef deque <uint> udeque;

typedef uint long long uint64;
typedef const uint64 cuint64;
typedef deque <uint64> u64deque;

#endif